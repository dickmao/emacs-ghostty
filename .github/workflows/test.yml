name: CI

on:
  pull_request:
    paths-ignore:
    - 'README**'
    - '**.rst'
    - '**.texi'
  push:
    paths-ignore:
    - 'README**'
    - '**.rst'
    - '**.texi'
    branches-ignore:
    - 'master'
    - 'main'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
    - uses: maxim-lobanov/setup-xcode@v1
      if: startsWith(runner.os, 'macOS')
      with:
        xcode-version: '26.3'

    - uses: actions/checkout@v4

    - uses: mlugg/setup-zig@v2.2.1
      with:
        version: 0.15.2

    - name: apt-get
      if: startsWith(runner.os, 'Linux')
      run: |
        sudo apt-get -yq update
        DEBIAN_FRONTEND=noninteractive sudo apt-get -yq install libxml2-dev libgccjit-11-dev emacs libtool libtool-bin libltdl-dev

    - name: brew-haha
      if: startsWith(runner.os, 'macOS')
      run: |
        brew install emacs libgccjit libxml2 libtool

    - name: test
      run: |
        make test
        make install
        # verify ghostty-vt-module.so loads correctly from install dir
        cd $(mktemp -d) && emacs --batch -l package -f package-initialize -l ghostty-vt
